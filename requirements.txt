# Core async HTTP (Ollama client)
httpx>=0.27.0

# Terminal UI
rich>=13.7.0
prompt_toolkit>=3.0.43

# Vector memory
chromadb>=0.5.0

# Web search (no API key required)
duckduckgo-search>=6.1.0

# Config
PyYAML>=6.0.1

# Optional: local embeddings (~80 MB, faster than Ollama embed)
# sentence-transformers>=3.0.0
